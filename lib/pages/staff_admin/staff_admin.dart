import 'package:flutter/material.dart';

class StaffAdminPage extends StatelessWidget {
  const StaffAdminPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Staff Admin Page"));
  }
}
