import 'package:flutter/material.dart';

class SubscriberPage extends StatelessWidget {
  const SubscriberPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Subscriber Page"));
  }
}
